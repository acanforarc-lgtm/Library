# Claude Code Instructions

## Branch Policy
- Always work directly on `main`
- Never create feature branches
- Always push directly to `main`
- Do not open pull requests

## Memory
- All memory must be written exclusively to the `Memory/` subfolder in the project root (syncs via Nextcloud)
- Never write memory to the `.claude/projects/` path
- MEMORY.md index lives at `Memory/MEMORY.md` in the project root
